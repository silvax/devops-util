---
  - hosts: tag_role_dialer
    sudo: true
    gather_facts: false

    tasks:
      - name: query the packages for the right version of glibc
        yum: name=glibc
        register: glibc_version

      - debug: var=glibc_version
